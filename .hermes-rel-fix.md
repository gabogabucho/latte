# Release race-safety fix evidence

## RED

- Command: `npm test -- --run tests/backend/release-workflows.test.ts`
- Expected result: failed (4 failures): all three workflows still had rejected `concurrency` blocks, and Windows did not retain the `.exe.blockmap` asset.

## GREEN

- Command: `npm test -- --run tests/backend/release-workflows.test.ts`
- Result: passed, 1 file / 6 tests.
- Command: `npm run typecheck:all`
- Result: passed.
- YAML/shell check: parsed all three workflows with `js-yaml`; extracted every `run` block and checked each platform script with `bash -n`.
- Diff check: `git diff --check` passed.

## Behavior covered

- No workflow-level concurrency block.
- Initial release lookup; artifact-free draft creation whose race failure is tolerated.
- Ten bounded lookup attempts with two-second waits and explicit terminal failure.
- Unconditional post-poll `gh release upload --clobber` with Linux, Windows (including blockmap), and macOS asset globs.
- Existing release notes and `v*` triggers retained.
